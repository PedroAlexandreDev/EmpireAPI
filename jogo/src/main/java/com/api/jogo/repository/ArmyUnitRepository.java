package com.api.jogo.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.api.jogo.entity.ArmyUnit;

public interface ArmyUnitRepository extends JpaRepository<ArmyUnit, Long> {

    
}
